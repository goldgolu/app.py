services:
  - type: web
    name: my-flask-app
    env: python
    pythonVersion: 3.9
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PORT
        value: 5000
      
