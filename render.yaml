services:
  - type: web
    name: my-flask-app  # अपनी ऐप का नाम दें
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PORT
        value: 5000
